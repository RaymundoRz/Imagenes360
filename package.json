{
  "name": "honda-360-extractor",
  "version": "1.0.0",
  "description": "Honda 360° Extractor - Sistema completo",
  "scripts": {
    "dev": "concurrently \"cd backend && python -m uvicorn app.main:app --reload --port 8000\" \"cd frontend && npm run dev\" \"cd backend/downloads && python -m http.server 8080\"",
    "install-all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "backend": "cd backend && python -m uvicorn app.main:app --reload --port 8000",
    "frontend": "cd frontend && npm run dev",
    "server": "cd backend/downloads && python -m http.server 8080"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}